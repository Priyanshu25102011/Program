command = input("do you want to shutdown (yes/no)")
def shutdown(command):
    if (command == "yes"):
      print("shutting down...")
    
    else:
       print("sorry!")

shutdown(command)

     
